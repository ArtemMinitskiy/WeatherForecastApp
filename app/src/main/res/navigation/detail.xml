<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
            xmlns:app="http://schemas.android.com/apk/res-auto"
            android:id="@+id/detail_nav_graph"
            app:startDestination="@id/detail_fragment">

    <fragment
            android:id="@+id/detail_fragment"
            android:name="com.example.weatherforecastapp.DetailFragment"
            android:label="DetailFragment">

        <argument
                android:name="tab_number"
                app:argType="integer"/>
        <argument
                android:name="some_extra_info"
                app:argType="string"/>
        <argument
            android:name="weather_item"
            app:argType="com.example.weatherforecastapp.WeatherItem"
            app:nullable="true" />
    </fragment>

</navigation>